-- MySQL Script generated by MySQL Workbench
-- Mon Dec 1 14:09:40 2025
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

-- -----------------------------------------------------
-- Schema PrimeHotel
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `PrimeHotel` DEFAULT CHARACTER SET utf8 ;
USE `PrimeHotel` ;

-- -----------------------------------------------------
-- Table `PrimeHotel`.`Clientes`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `PrimeHotel`.`Clientes` (
  `idClientes` INT NOT NULL AUTO_INCREMENT,
  `nome_completo` VARCHAR(100) NOT NULL, -- Corrigido para "nome_completo" (sem traço)
  `email` VARCHAR(80) NOT NULL,
  `telefone` CHAR(12) NOT NULL, -- Corrigido para "telefone" (minúscula)
  `dt_contato` DATE NOT NULL,
  `dt_reserva` DATE NOT NULL,
  PRIMARY KEY (`idClientes`)
) ENGINE = InnoDB;

-- -----------------------------------------------------
-- Table `PrimeHotel`.`Reservas`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `PrimeHotel`.`Reservas` (
  `idReservas` INT NOT NULL AUTO_INCREMENT, -- Corrigido para "idReservas" (minúsculo)
  `hospede` VARCHAR(100) NOT NULL,
  `periodo` DATE NOT NULL, -- Apenas a data de início da reserva
  `tipo_apto` CHAR(5) NOT NULL, -- Corrigido para "tipo_apto" (sem traço)
  `valor` DECIMAL(10, 2) NOT NULL, -- Adicionado precisão para o campo DECIMAL
  `quant_pes` INT NOT NULL,
  `solicitac` TEXT NULL, -- LONGTEXT é um tipo de dado desnecessariamente grande para esta coluna. TEXT é mais apropriado.
  PRIMARY KEY (`idReservas`)
) ENGINE = InnoDB;

-- -----------------------------------------------------
-- Table `PrimeHotel`.`Clientes_has_Reservas`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `PrimeHotel`.`Clientes_has_Reservas` (
  `Clientes_idClientes` INT NOT NULL,
  `Reservas_idReservas` INT NOT NULL, -- Corrigido para "Reservas_idReservas" (minúsculo)
  PRIMARY KEY (`Clientes_idClientes`, `Reservas_idReservas`),
  INDEX `fk_Clientes_has_Reservas_Reservas1_idx` (`Reservas_idReservas` ASC) VISIBLE,
  INDEX `fk_Clientes_has_Reservas_Clientes_idx` (`Clientes_idClientes` ASC) VISIBLE,
  CONSTRAINT `fk_Clientes_has_Reservas_Clientes`
    FOREIGN KEY (`Clientes_idClientes`)
    REFERENCES `PrimeHotel`.`Clientes` (`idClientes`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_Clientes_has_Reservas_Reservas1`
    FOREIGN KEY (`Reservas_idReservas`)
    REFERENCES `PrimeHotel`.`Reservas` (`idReservas`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION
) ENGINE = InnoDB;

-- -----------------------------------------------------
-- Inserção de dados em `Clientes`
-- -----------------------------------------------------
-- O comando `INSERT` precisa de todas as colunas NOT NULL.
-- As datas foram adicionadas como exemplo.
INSERT INTO `Clientes` (`nome_completo`, `email`, `telefone`, `dt_contato`, `dt_reserva`)
VALUES 
('Jose Santos', 'santos.silva@email.com', '11987654321', '2025-11-20', '2025-12-10'),
('Fabio Silva', 'fabio.silva@email.com', '11987654322', '2025-11-21', '2025-12-12'),
('Alice Souza', 'alice.souza@email.com', '11987654323', '2025-11-22', '2025-12-15'),
('Joelma Santos', 'joelma.santos@email.com', '11987654324', '2025-11-23', '2025-12-18'),
('Alexandre Ribeiro', 'a.ribeiro@email.com', '11987654325', '2025-11-24', '2025-12-20');

-- -----------------------------------------------------
-- Consultas (SELECT)
-- -----------------------------------------------------
-- Corrigido o nome da coluna `nome-completo` para `nome_completo`
-- Corrigido o nome da tabela `idclientes` para `Clientes`
-- Adicionado a cláusula `WHERE` corretamente
SELECT `nome_completo`, `email`
FROM `Clientes`
WHERE `idClientes` > 0 -- Exemplo de condição
ORDER BY `nome_completo`;

-- Corrigido o nome da tabela `idreservas` para `Reservas`
-- Adicionado a cláusula `WHERE` corretamente
SELECT `hospede`
FROM `Reservas`
WHERE `idReservas` > 0 -- Exemplo de condição
ORDER BY `hospede`;

-- -----------------------------------------------------
-- Atualizações (UPDATE)
-- -----------------------------------------------------
-- O comando `UPDATE` precisa de aspas para valores de string e uma cláusula WHERE válida.
-- Os valores "novo_valor" são incorretos e foram removidos.
-- O nome da tabela e coluna foram corrigidos.
UPDATE `Reservas`
SET `hospede` = 'Jose Silva Santos'
WHERE `hospede` = 'Jose Santos';

UPDATE `Reservas`
SET `hospede` = 'Fabio Soares Silva'
WHERE `hospede` = 'Fabio Silva';

UPDATE `Reservas`
SET `hospede` = 'Alexandre Ribeiro Soares'
WHERE `hospede` = 'Alexandre Ribeiro';

-- -----------------------------------------------------
-- Exclusões (DELETE)
-- -----------------------------------------------------
-- O comando `DELETE` precisa de uma cláusula WHERE válida e um valor de ID para exclusão.
-- As exclusões por nome não são confiáveis e foram substituídas por `idClientes`.
DELETE FROM `Clientes` WHERE `idClientes` = 3; -- Exclui "Alice Souza"
DELETE FROM `Clientes` WHERE `idClientes` = 4; -- Exclui "Joelma Santos"

-- -----------------------------------------------------
-- Finalização
-- -----------------------------------------------------
SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;

